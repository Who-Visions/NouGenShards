# Fleet log — 2026-08-16

Everything whoart learned since the last relay, published because
**shards do not travel**. The vault lives at `~/.nougen/shards` on
whoart; the other machines cannot read it. This file is the transport,
and it is the record you should trust over any summary in a chat window.

3 entries, oldest first. Verbatim from the vault — not re-summarised.

Covers shards captured after `2026-08-16T23:14:59Z`.

---

## 23:15Z — CORRECTION: Ollama /api/tags a

CORRECTION: Ollama /api/tags and /api/ps are inventory signals, not proof of inference health. The dream lane must run a real chat completion preflight after model selection and abort before writing any digest if generation fails. On WhoArt the missing llama-server runtime was repaired on 2026-08-16 and Ollama auto_update_enabled was set to 0.

---

## 23:15Z — CORRECTION: A rolling 24-hour

CORRECTION: A rolling 24-hour digest window can age missed sources out while runtime repair is underway. tools/dream_digest_run.py accepts explicit JSONL source paths for targeted backfill without re-digesting handoffs. The 2026-08-16 recovery processed all 21 originally missed transcripts plus 7 handoffs into 22 artifacts with zero inference errors and zero surviving secret-pattern matches.

---

## 23:15Z — DECISION: Evidence assurance r

DECISION: Evidence assurance routes through Iris and returns VERIFIED, CONTRADICTED, UNCERTAIN, or UNVERIFIED with confidence, rationale, evidence used, and caveats. Assurance labels always require operator review and never auto-promote or delete shards. Inputs and outputs are secret-pattern redacted and invalid Iris output fails closed to UNVERIFIED.

---

<!-- relay-shards through=2026-08-16T23:15:02.263682Z machine=whoart generated-by=relay-shards -->
